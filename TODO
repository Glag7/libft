TODO
compilation taille size_t

move header

arr :
chrarr_int : 2* faster
qsort : spare the stack (+ ??? partitioning)
_len : use custom funcs

dlst :
betterswap

lst :
(c11)
(lst_insert)
(dlst)

math :
sqrt : use * instead of /

mem :
align + restrict
memchr : use longptr

str :
strlen : use longptr
rework

tstr :
add str funcs

(trees)
(bigint)
PRINT MEMORY
