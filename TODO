TODO
better makefile, better messages (babonnet)
compilation taille size_t

libft : ifdef heqders, prototype

arr :
chrarr_int : 2* faster
qsort : spare the stack (+ ??? partitioning)
_len : use custom funcs

dlst :
betterswap

lst :
(c11)
(lst_insert)
(dlst)

math :
sqrt : use * instead of /

mem :
align + restrict
memchr : use longptr

print :
(ft_printf)

str :
strlen : use longptr
rework

tstr :
add str funcs
use ptr instead of struct ? (union ?)

(trees)
(bigint)
PRINT MEMORY
